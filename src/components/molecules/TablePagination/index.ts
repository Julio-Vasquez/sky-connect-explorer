/**
 * @file Barrel export for the Pagination component.
 * @description Re-exports the default Pagination component from its module for simplified imports.
 *
 * @example
 * // Instead of:
 * import Pagination from '@components/molecules/pagination/TablePagination'
 *
 * // You can now do:
 * import Pagination from '@components/molecules/TablePagination'
 */

export { default } from './TablePagination'
