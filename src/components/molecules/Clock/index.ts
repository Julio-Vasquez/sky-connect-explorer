/**
 * @file Barrel export for the Clock component.
 * @description Re-exports the default Clock component from its module for simplified imports.
 *
 * @example
 * // Instead of:
 * import Clock from '@components/molecules/Clock/Clock'
 *
 * // You can now do:
 * import Clock from '@components/molecules/Clock'
 */

export { default } from './Clock'
