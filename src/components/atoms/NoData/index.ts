/**
 * @file Barrel export for the NoData component.
 * @description Re-exports the default NoData component from its module for simplified imports.
 *
 * @example
 * // Instead of:
 * import NoData from '@components/atoms/NoData/NoData'
 *
 * // You can now do:
 * import NoData from '@components/atoms/NoData'
 */

export { default } from './NoData'
