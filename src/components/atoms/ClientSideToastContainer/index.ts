/**
 * @file Barrel export for the ClientSideToastContainer component.
 * @description Re-exports the default ClientSideToastContainer component from its module for simplified imports.
 *
 * @example
 * // Instead of:
 * import ClientSideToastContainer from '@components/atoms/ClientSideToastContainer/ClientSideToastContainer'
 *
 * // You can now do:
 * import ClientSideToastContainer from '@components/atoms/ClientSideToastContainer'
 */

export { default } from './ClientSideToastContainer'
