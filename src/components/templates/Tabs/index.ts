/**
 * @file Barrel export for the Tabs component.
 * @description Re-exports the default Tabs component from its module for simplified imports.
 *
 * @example
 * // Instead of:
 * import Tabs from '@components/template/Tabs/Tabs'
 *
 * // You can now do:
 * import Tabs from '@components/template/Tabs'
 */

export { default } from './Tabs'
