/**
 * @file Barrel export for the Home component.
 * @description Re-exports the default Home component from its module for simplified imports.
 *
 * @example
 * // Instead of:
 * import Home from '@components/template/Home/Home'
 *
 * // You can now do:
 * import Home from '@components/template/Home'
 */
export { default } from './Home'
