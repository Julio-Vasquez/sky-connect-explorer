/**
 * @file Barrel export for the SearchBar component.
 * @description Re-exports the default SearchBar component from its module for simplified imports.
 *
 * @example
 * // Instead of:
 * import SearchBar from '@components/organism/SearchBar/SearchBar'
 *
 * // You can now do:
 * import SearchBar from '@components/organism/SearchBar'
 */
export { default } from './SearchBar'
