/**
 * @file Barrel export for the AppHeader component.
 * @description Re-exports the default AppHeader component from its module for simplified imports.
 *
 * @example
 * // Instead of:
 * import AppHeader from '@components/organism/AppHeader/AppHeader'
 *
 * // You can now do:
 * import AppHeader from '@components/organism/AppHeader'
 */

export { default } from './AppHeader'
