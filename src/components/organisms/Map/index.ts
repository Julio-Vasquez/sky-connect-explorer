/**
 * @file Barrel export for the Map component.
 * @description Re-exports the default Map component from its module for simplified imports.
 *
 * @example
 * // Instead of:
 * import Map from '@components/organism/Map/Map'
 *
 * // You can now do:
 * import Map from '@components/organism/Map'
 */
export { default } from './Map'
